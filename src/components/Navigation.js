import React from "react";
import { Link } from "@reach/router";

const Navigation = () => {
  return (
    <div>
      <h1>Navigation</h1>
      <Link to="/">HOME</Link>
    </div>
  );
};

export default Navigation;
